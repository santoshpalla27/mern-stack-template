# ============================================
# SERVER CONFIGURATION (REQUIRED)
# ============================================
PORT=5000
NODE_ENV=production

# ============================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================
# MongoDB Connection URI
# Examples:
#   Local: mongodb://localhost:27017/devops-demo
#   Docker: mongodb://mongodb:27017/devops-demo
#   Atlas: mongodb+srv://username:password@cluster.mongodb.net/devops-demo
#   Replica Set: mongodb://host1:27017,host2:27017,host3:27017/devops-demo?replicaSet=myReplicaSet
MONGO_URI=mongodb://mongodb:27017/devops-demo

# ============================================
# CACHE CONFIGURATION (OPTIONAL)
# ============================================
# Redis Connection URI - Leave empty to skip Redis
# Examples:
#   Local: redis://localhost:6379
#   Docker: redis://redis:6379
#   Password: redis://:password@redis:6379
#   AWS ElastiCache: redis://your-elasticache-endpoint:6379
#   Cluster: redis://node1:6379,node2:6379,node3:6379
REDIS_URI=redis://redis:6379

# ============================================
# SECURITY CONFIGURATION (REQUIRED)
# ============================================
# CORS - Allowed origins (comma-separated for multiple)
# Examples:
#   Single: http://localhost:3000
#   Multiple: http://localhost:3000,http://example.com
#   All (not recommended for production): *
CORS_ORIGIN=http://localhost:3000

# ============================================
# LOGGING CONFIGURATION (OPTIONAL)
# ============================================
# Log levels: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info

# ============================================
# CONNECTION RETRY CONFIGURATION (OPTIONAL)
# ============================================
# MongoDB retry settings
MONGO_MAX_RETRIES=10
MONGO_RETRY_DELAY=5000

# Redis retry settings
REDIS_MAX_RETRIES=10
REDIS_RETRY_DELAY=5000

# ============================================
# DEPLOYMENT CONFIGURATION (OPTIONAL)
# ============================================
# Platform identifier for architecture endpoint
DEPLOYMENT_PLATFORM=Docker Compose

# Orchestration type
ORCHESTRATION=Docker Compose

# Number of replicas (for scaling info)
BACKEND_REPLICAS=1
FRONTEND_REPLICAS=1

# Load balancer type
LOAD_BALANCER=none

# ============================================
# NOTES
# ============================================
# REQUIRED variables must be set for the app to work
# OPTIONAL variables have sensible defaults
# 
# For production:
# - Use strong passwords in MongoDB/Redis URIs
# - Set specific CORS_ORIGIN (not *)
# - Set NODE_ENV=production
# - Use connection strings with authentication
# - Consider using secrets management (AWS Secrets Manager, HashiCorp Vault)
#
# Examples for different environments:
#
# Development:
#   NODE_ENV=development
#   MONGO_URI=mongodb://localhost:27017/devops-demo
#   REDIS_URI=redis://localhost:6379
#   CORS_ORIGIN=http://localhost:3000
#
# Docker Compose:
#   NODE_ENV=production
#   MONGO_URI=mongodb://mongodb:27017/devops-demo
#   REDIS_URI=redis://redis:6379
#   CORS_ORIGIN=http://localhost:3000
#
# Kubernetes:
#   NODE_ENV=production
#   MONGO_URI=mongodb://mongo-service:27017/devops-demo
#   REDIS_URI=redis://redis-service:6379
#   CORS_ORIGIN=http://frontend-service:3000
#
# AWS (with ElastiCache & DocumentDB):
#   NODE_ENV=production
#   MONGO_URI=mongodb://username:password@docdb-cluster.cluster-xxx.us-east-1.docdb.amazonaws.com:27017/devops-demo?tls=true&replicaSet=rs0&readPreference=secondaryPreferred&retryWrites=false
#   REDIS_URI=redis://your-elasticache.cache.amazonaws.com:6379
#   CORS_ORIGIN=https://yourdomain.com